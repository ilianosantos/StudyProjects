# **Instructions to build and run the project**
Run the backend server, check if you have node and npm installed in your pc and then 
in this base directory, run in command prompt the following commands:
* npm install
* npm run build
* npm run start

Go to browser then search for localhost:8081